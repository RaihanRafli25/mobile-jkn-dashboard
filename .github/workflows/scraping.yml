name: Auto Scraping Mobile JKN

on:
  schedule:
    - cron: '0 0 1 */6 *'
  workflow_dispatch:

jobs:
  scraping:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements_scraping.txt

      - name: Debug secrets
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          if [ -z "$SUPABASE_URL" ]; then
            echo "SUPABASE_URL kosong!"
          else
            echo "SUPABASE_URL terisi"
          fi
          if [ -z "$SUPABASE_KEY" ]; then
            echo "SUPABASE_KEY kosong!"
          else
            echo "SUPABASE_KEY terisi"
          fi

      - name: Jalankan scraping
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: python scraping.py
